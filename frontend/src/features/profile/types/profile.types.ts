/** Profile details for the current user. */
export interface UserProfile {
  /** Profile identifier. */
  id: string;
  /** Display name. */
  name: string;
  /** Short profile biography. */
  bio: string;
}
